# OpenSpec area

- `specs/` contains accepted current-state behavior.
- `changes/` contains active change proposals and their planning artifacts.
- The workshop uses a controlled flow: explore, create/continue, human planning approval, apply, verify and archive.

Do not create a behavioral change during LAB 1. Follow the release gate in the relevant lab instruction.
